node_modules
.env
.env.local
.env.*.local
*.log
.DS_Store
dist/
build/
*.key
*.pem
*.cert
secrets/
.secrets/

# Local installer scripts that contain real credentials — NEVER commit these
install-local.sh

# Jamf/MDM scripts with gateway URL and API key — configure in Jamf Pro or keep local, never commit
jamf*.sh
*jamf*.sh
